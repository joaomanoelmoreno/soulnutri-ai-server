services:
  - type: web
    name: soulnutri-ai-server
    runtime: python
    plan: free
    region: oregon

    buildCommand: |
      pip install --upgrade pip
      pip install -r requirements.txt

    startCommand: |
      python -m uvicorn main:app --host 0.0.0.0 --port $PORT --log-level info

    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"

      # força modo embeddings (Servidor B)
      - key: RECO_MODE
        value: embeddings

      # garante caminho de dados
      - key: DATA_DIR
        value: /opt/render/project/src/data
